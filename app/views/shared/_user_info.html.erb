<%= link_to gravatar_for(current_user, size: Settings.max_name_length), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to t("micropost.user"), current_user %></span>
<span><%= pluralize(current_user.microposts.count, t("micropost.micropost")) %></span>
