<li>
  <%= gravatar_for user, size: Settings.default_gravatar_size %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
    | <%= link_to t("user.action"), user, data: { "turbo-method": :delete,
      turbo_confirm: t("user.question") } %>
  <% end %>
</li>
